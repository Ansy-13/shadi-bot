# Core Chainlit
chainlit==2.6.2

# API & LLM support
openai==0.28.0
anthropic==0.58.2
python-dotenv==1.1.1
requests==2.32.3

# FastAPI (used internally by Chainlit sometimes)
fastapi==0.115.14
uvicorn==0.35.0

# Optional: Data handling & utility
pandas==2.2.3
numpy==2.2.3

# Visualization (optional - only keep if you're using plotly/streamlit)
plotly==6.0.0
streamlit==1.42.2

# Optional: For OpenTelemetry (used for tracing / logging in complex apps)
opentelemetry-sdk==1.34.1
opentelemetry-exporter-otlp==1.34.1

# OS-specific (only install on Windows, not on Railway)
pywin32==311; sys_platform == "win32"
